name: grocery_guardian
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.5
  android_intent_plus: ^5.3.0
  shared_preferences: ^2.2.3
  http: ^1.2.1
  package_info_plus: ^8.3.0
  apk_installer: ^0.0.4  # Use the latest available version on pub.dev
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

flutter_icons:
  android: true
  ios: true
  image_path: "icon.png"
