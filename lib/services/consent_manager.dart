// Placeholder consent manager.
// To implement UMP, add the `user_messaging_platform` plugin or use UMP APIs
// available in google_mobile_ads when configured, and wire the consent flow here.

class ConsentManager {
  static Future<void> requestConsentAndShowIfRequired() async {
    // TODO: Integrate Google's User Messaging Platform (UMP) here.
    // Keep this a no-op by default so builds succeed without extra setup.
    return;
  }
}
